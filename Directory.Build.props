<Project>
  <PropertyGroup>
    <RestoreProjectStyle Condition="'$(MSBuildProjectExtension)' == '.csproj' AND '$(TargetFramework)' == ''">PackageReference</RestoreProjectStyle>
  </PropertyGroup>
</Project>
